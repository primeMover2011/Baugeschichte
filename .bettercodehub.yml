# https://bettercodehub.com

component_depth: 1
languages:
- name: cpp
  production:
    exclude:
      - /doc/.*
      - /src/qml/.
      - /tests/.*
      - /translations/.*
  test:
    include:
      - /tests/.*\.cpp
