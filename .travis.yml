sudo: required
dist: trusty

language: cpp
compiler: gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y qtbase5-dev-tools qtdeclarative5-dev qt5-default
  - sudo apt-get install -y libqt5quickcontrols2-5 libqt5quicktest5 qtlocation5-dev qtpositioning5-dev

script:
  - qmake . -recursive
  - make
  - make check

after_success:
  - echo "success"
 
after_failure:
- echo "failure"
